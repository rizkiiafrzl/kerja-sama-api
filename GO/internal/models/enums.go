package models

// Status enum values
const (
	StatusActive   = "active"
	StatusInactive = "inactive"
)

// User role enum values
const (
	UserRoleAdmin  = "admin"
	UserRoleViewer = "viewer"
)

// Admin role enum values
const (
	AdminRoleSuperadmin = "superadmin"
	AdminRoleOperator   = "operator"
)

// TK status enum values
const (
	TKStatusAktif    = "aktif"
	TKStatusNonaktif = "nonaktif"
	TKStatusUnknown  = "unknown"
)
